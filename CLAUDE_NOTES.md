# ملاحظات المراجعة - منصة MIX

## القرار: Firebase/Firestore هي قاعدة البيانات المعتمدة الوحيدة

### التشخيص
كان في المشروع نظامين تخزين متوازيين:
1. **Firebase/Firestore** (`src/lib/firebaseSync.ts`, `src/lib/firebaseData.ts`) — Real-Time حقيقي عبر `onSnapshot`، ومتصل فعليًا بكل الصفحات.
2. **MongoDB عبر REST** (`server/`, `api/index.js`, `src/lib/dbAPI.ts`) — محاولة هجرة غير مكتملة، كانت بتشتغل بـ Polling، وعنوانها (`VITE_API_URL`) غير معرّف في أي `.env`، فكانت بتحاول تتصل بـ `http://localhost:3001` حتى في الإنتاج وتفشل بصمت. الاثنان كانا يكتبان على نفس مفاتيح `localStorage`، وهو مصدر تعارضات محتملة.

### ما تم تنفيذه في هذه الحزمة
- تعطيل استدعاء `dbAPI.init()` في `src/App.tsx` (الاستيراد والتهيئة) مع توثيق السبب في الكود. Firestore الآن هو مصدر البيانات الوحيد الفعّال.
- حذف المجلدات المكررة من النسخة القديمة: `remix_extracted*`, `dist-backup`, أرشيفات `.zip` قديمة، و`node_modules` (يُعاد تثبيتها بـ `npm install`).
- لم يتم حذف أي كود أو ملف بيانات فعلي غير هذه المخلفات — لا توجد بيانات حقيقية تأثرت.

### لم يُلمس بعد (متروك كما هو حتى قرارك)
- مجلد `server/` وملف `api/index.js` (كود Mongo) لسه موجودين في المشروع بدون حذف، تحسبًا لو احتجتهم لاحقًا كمرجع. ينصح بحذفهم نهائيًا بعد التأكد.
- `render.yaml` (نشر خدمة Mongo على Render) لسه موجود — يفضّل حذفه لو مش هتستخدم Mongo نهائيًا.

## المراحل التالية (لم تبدأ بعد)
1. تقوية نواة الـ Real-Time (توحيد الأحداث، إزالة أي بقايا اعتماد على polling).
2. لوحة الإدارة: البنرات → الأقسام → المتاجر → الصفحات.
3. لوحة التاجر: القوالب الديناميكية حسب نوع النشاط.
4. نموذج الطلب القابل للتخصيص + طرق الدفع + الطلبات.

هذا مشروع كبير (AdminDashboard.tsx وMerchantDashboard.tsx كل واحد فيهم أكتر من 3700 سطر) — الأفضل إكماله على مراحل متتالية، كل مرحلة بيتم اختبارها قبل الانتقال للي بعدها.
